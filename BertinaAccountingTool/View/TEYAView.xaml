<UserControl x:Class="BertinaAccountingTool.View.TEYAView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BertinaAccountingTool.View"
             xmlns:viewmodel="clr-namespace:BertinaAccountingTool.BusinessLogic.ViewModel"
             d:DataContext="{d:DesignInstance Type=viewmodel:TEYAViewModel}"
             mc:Ignorable="d" d:DesignHeight="450" d:DesignWidth="800" d:Background="White">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="300"/>
            <ColumnDefinition Width="70"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Content="Vevői számlák:" Margin="3"/>
        <TextBox x:Name="tbBuyerInvoives" Text="{Binding BuyerInvoives, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="1" Margin="3"/>
        <Button Content="Tallózás" Grid.Column="1" Grid.Row="1" Margin="3" Command="{Binding SourceBrowseCommand}" CommandParameter="{Binding ElementName=tbBuyerInvoives}"/>

        <Label Content="Tranzakciók:" Margin="3" Grid.Row="2"/>
        <TextBox x:Name="tbTransactions" Text="{Binding Transactions, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="3" Margin="3"/>
        <Button Content="Tallózás" Grid.Column="1" Grid.Row="3" Margin="3" Command="{Binding SourceBrowseCommand}" CommandParameter="{Binding ElementName=tbTransactions}"/>

        <Label Content="Kimenet:" Margin="3" Grid.Row="4"/>
        <TextBox x:Name="tbTransactionsOutput" Text="{Binding TransactionsOutput, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Grid.Row="5" Margin="3"/>
        <Button Content="Tallózás" Grid.Column="1" Grid.Row="5" Margin="3" Command="{Binding SourceBrowseCommand}" CommandParameter="{Binding ElementName=tbTransactionsOutput}"/>

        <Button Content="Betöltés" Grid.Row="6" Grid.ColumnSpan="2" Margin="3" Command="{Binding LoadDataCommand}"/>

        <Button Content="Fájba írás" Grid.Row="7" Grid.ColumnSpan="2" Margin="3" Command="{Binding CreateTransactionsOutputCommand}"/>

        <DataGrid Grid.Column="2" Grid.RowSpan="99" ItemsSource="{Binding ErrorTransactions}" AutoGenerateColumns="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Szám" Binding="{Binding AccountNumber}" />
                <DataGridTextColumn Header="Összeg" Binding="{Binding Value}" />
                <DataGridTextColumn Header="Számla kelte" Binding="{Binding Date}" />
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>
